load("//build/kernel/kleaf:kernel.bzl", "ddk_headers")
load("@mgk_info//:kernel_version.bzl", "kernel_version")

package(
    default_visibility = [
        "//visibility:public",
    ],
)

filegroup(
    name = "srcs",
    srcs = glob([
        "**/*.h",
    ]),
)

ddk_headers(
    name = "retention_header",
    hdrs = glob([ "**/*.h" ]),
    includes = [ "inc" ],
    visibility = [ "//visibility:public" ],
)